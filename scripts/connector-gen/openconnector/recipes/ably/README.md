# Ably reviewed recipe

Uses Ably REST `/stats` with split key name and secret fields rendered as HTTP Basic authentication. Includes read-only healthcheck and statistics operations. Live authentication remains unverified.
