# Bugsnag recipe

Read-only Bugsnag API recipe using a personal auth token in the documented `Authorization: token ...` header. Operations are healthcheck and organization listing; live authentication remains unverified. Source is Apache-2.0 licensed upstream OpenConnector at the pinned revision.
