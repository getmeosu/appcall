{
  "schemaVersion": 1,
  "id": "healthcheck-unauthorized",
  "operation": "healthcheck",
  "input": {},
  "credentials": {
    "apiKey": "bad-daffy-key"
  },
  "evidence": {
    "sourceRefs": [
      "request-auth"
    ],
    "documentationUrls": [
      "https://docs.daffy.org/auth"
    ],
    "basis": "Pinned source maps 401/403 on execute to an upstream error. Official docs require X-Api-Key; they do not publish a 401 schema, so the body uses the source error/message/detail extractor path."
  },
  "exchanges": [
    {
      "request": {
        "method": "GET",
        "url": "https://public.daffy.org/v1/users/me",
        "headers": {
          "x-api-key": "bad-daffy-key",
          "accept": "application/json"
        },
        "body": null
      },
      "response": {
        "status": 401,
        "headers": {
          "content-type": "application/json"
        },
        "bodyFile": "../responses/unauthorized.json"
      }
    }
  ],
  "expected": {
    "kind": "error",
    "code": "CONNECTOR_UPSTREAM_ERROR"
  }
}
