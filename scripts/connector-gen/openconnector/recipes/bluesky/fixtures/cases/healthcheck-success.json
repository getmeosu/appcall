{
  "schemaVersion": 1,
  "id": "healthcheck-success",
  "operation": "healthcheck",
  "input": {},
  "credentials": {
    "apiKey": "fixturekey"
  },
  "evidence": {
    "sourceRefs": ["create-session", "handler-get-timeline", "request-auth"],
    "documentationUrls": [
      "https://docs.bsky.app/docs/api/com-atproto-server-create-session"
    ],
    "basis": "HOLD stand-in: GET /xrpc/app.bsky.feed.getTimeline after a session JWT. Native cannot mint that JWT from an app password."
  },
  "exchanges": [
    {
      "request": {
        "method": "GET",
        "url": "https://bsky.social/xrpc/app.bsky.feed.getTimeline",
        "headers": {
          "authorization": "Bearer fixturekey",
          "accept": "application/json"
        },
        "body": null
      },
      "response": {
        "status": 200,
        "headers": {
          "content-type": "application/json"
        },
        "bodyFile": "../responses/timeline.json"
      }
    }
  ],
  "expected": {
    "kind": "success",
    "resultFile": "../expected/healthcheck-success.json"
  }
}
