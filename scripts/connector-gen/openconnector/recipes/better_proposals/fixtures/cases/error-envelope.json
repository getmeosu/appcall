{
  "schemaVersion": 1,
  "id": "healthcheck-error-envelope",
  "operation": "healthcheck",
  "input": {},
  "credentials": {
    "apiKey": "fixture-token"
  },
  "evidence": {
    "sourceRefs": [
      "get-settings"
    ],
    "documentationUrls": [
      "https://betterproposals.io/resources/api/"
    ],
    "basis": "Official docs say every JSON body has status success or error. Native bodyErrorPaths treat a non-empty message as the false-success error signal because status success would trip a string path."
  },
  "exchanges": [
    {
      "request": {
        "method": "GET",
        "url": "https://api.betterproposals.io/settings",
        "headers": {
          "accept": "application/json",
          "bptoken": "fixture-token"
        },
        "body": null
      },
      "response": {
        "status": 200,
        "headers": {
          "content-type": "application/json"
        },
        "bodyFile": "../responses/unauthorized.json"
      }
    }
  ],
  "expected": {
    "kind": "error",
    "code": "CONNECTOR_UPSTREAM_ERROR"
  }
}
