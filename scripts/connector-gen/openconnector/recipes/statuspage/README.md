# Statuspage reviewed recipe

Read-only Statuspage international API v1 recipe. Configure an API token; requests use `Authorization: OAuth {{apiKey}}` against the fixed `api.statuspage.io` host. Operations are credential-only healthcheck, page listing, and component listing. Live authentication remains unverified. Upstream source is the pinned OpenConnector repository; see recipe provenance for attribution.
