# Ably Control reviewed recipe

Uses the Ably Control API with a personal access token rendered as Bearer authentication. Healthcheck calls `/v1/me`; account statistics calls `/v1/accounts/{accountId}/stats`. Live authentication remains unverified.
