{
  "schemaVersion": 1,
  "id": "healthcheck-success",
  "operation": "healthcheck",
  "input": {},
  "credentials": {
    "apiKey": "fixture-gem-key"
  },
  "evidence": {
    "sourceRefs": ["credential-validate", "handler-list-users", "request-auth"],
    "documentationUrls": ["https://api.gem.com/v0/reference"],
    "basis": "Official CRM API authenticates with X-API-Key on GET /v0/users. Pinned validator probes page_size=1. Independent expected URL is https://api.gem.com/v0/users?page_size=1."
  },
  "exchanges": [
    {
      "request": {
        "method": "GET",
        "url": "https://api.gem.com/v0/users?page_size=1",
        "headers": {
          "x-api-key": "fixture-gem-key",
          "accept": "application/json"
        },
        "body": null
      },
      "response": {
        "status": 200,
        "headers": {
          "content-type": "application/json"
        },
        "bodyFile": "../responses/users.json"
      }
    }
  ],
  "expected": {
    "kind": "success",
    "resultFile": "../expected/users-health.json"
  }
}
