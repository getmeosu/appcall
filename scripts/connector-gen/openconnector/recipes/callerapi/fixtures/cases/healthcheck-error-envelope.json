{
  "schemaVersion": 1,
  "id": "healthcheck-error-envelope",
  "operation": "healthcheck",
  "input": {},
  "credentials": {
    "apiKey": "fixturekey"
  },
  "evidence": {
    "fixture": {
      "status": "supplied"
    },
    "live": {
      "status": "unverified"
    },
    "sourceRefs": [
      "provider-error"
    ],
    "documentationUrls": [
      "https://docs.callerapi.com/quickstart-1338012m0"
    ],
    "basis": "Pinned runtime treats HTTP 200 JSON with a non-empty error string as failure. Native bodyErrorPaths [error] demotes it. status is not used because success status is the non-empty string success."
  },
  "exchanges": [
    {
      "request": {
        "method": "GET",
        "url": "https://api.callerapi.com/api/me",
        "headers": {
          "x-auth": "fixturekey",
          "accept": "application/json"
        },
        "body": null
      },
      "response": {
        "status": 200,
        "headers": {
          "content-type": "application/json"
        },
        "bodyFile": "../responses/healthcheck-error-envelope-0.json"
      }
    }
  ],
  "expected": {
    "kind": "error",
    "code": "CONNECTOR_UPSTREAM_ERROR"
  }
}
