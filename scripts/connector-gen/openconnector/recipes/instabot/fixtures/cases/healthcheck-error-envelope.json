{
  "schemaVersion": 1,
  "id": "healthcheck-error-envelope",
  "operation": "healthcheck",
  "input": {},
  "credentials": {
    "apiKey": "fixture-api-key",
    "masterApiKey": "fixture-master-key"
  },
  "evidence": {
    "fixture": {
      "status": "supplied"
    },
    "live": {
      "status": "unverified"
    },
    "basis": "Official docs say apiStatusMessage is omitted on success. Native bodyErrorPaths treat a non-empty apiStatusMessage as the HTTP 200 false-success signal because apiStatusCode Success would trip a string path."
  },
  "exchanges": [
    {
      "request": {
        "method": "GET",
        "url": "https://api.instabot.io/v1/",
        "headers": {
          "Accept": "application/json",
          "Authorization": "X-Instabot-Master-Api-Key fixture-master-key",
          "X-Instabot-Api-Key": "fixture-api-key"
        },
        "body": null
      },
      "response": {
        "status": 200,
        "headers": {
          "content-type": "application/json"
        },
        "bodyFile": "../responses/healthcheck-error-envelope-0.json"
      }
    }
  ],
  "expected": {
    "kind": "error",
    "code": "CONNECTOR_UPSTREAM_ERROR"
  }
}
