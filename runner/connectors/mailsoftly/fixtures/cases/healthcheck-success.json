{
  "schemaVersion": 1,
  "id": "healthcheck-success",
  "operation": "healthcheck",
  "input": {},
  "credentials": {
    "apiKey": "fixturekey"
  },
  "evidence": {
    "sourceRefs": [
      "handler-get-contact-lists",
      "request-transport"
    ],
    "documentationUrls": [
      "https://mailsoftly.com/api-documentation/"
    ],
    "basis": "Official Mailsoftly v3 authenticates by sending the API token as the Authorization header value (not Bearer) on GET https://app.mailsoftly.com/api/v3/get_contact_lists. Healthcheck uses this cheap list read rather than POST /authentication."
  },
  "exchanges": [
    {
      "request": {
        "method": "GET",
        "url": "https://app.mailsoftly.com/api/v3/get_contact_lists",
        "headers": {
          "authorization": "fixturekey",
          "accept": "application/json"
        },
        "body": null
      },
      "response": {
        "status": 200,
        "headers": {
          "content-type": "application/json"
        },
        "bodyFile": "../responses/healthcheck-success-0.json"
      }
    }
  ],
  "expected": {
    "kind": "success",
    "resultFile": "../expected/healthcheck-success.json"
  }
}
