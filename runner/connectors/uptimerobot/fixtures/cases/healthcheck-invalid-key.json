{
  "schemaVersion": 1,
  "id": "healthcheck-invalid-key",
  "operation": "healthcheck",
  "input": {},
  "credentials": {
    "apiKey": "fixture-secret"
  },
  "evidence": {
    "sourceRefs": [
      "handler-stat-error",
      "error-message"
    ],
    "documentationUrls": [
      "https://uptimerobot.com/api/legacy/"
    ],
    "basis": "UptimeRobot v2 returns HTTP 200 with stat=fail and error.type=invalid_key for a bad api_key; pinned executors treat that envelope as failure. bodyErrorPaths [error] must demote it to CONNECTOR_UPSTREAM_ERROR."
  },
  "exchanges": [
    {
      "request": {
        "method": "POST",
        "url": "https://api.uptimerobot.com/v2/getAccountDetails",
        "headers": {
          "content-type": "application/x-www-form-urlencoded",
          "accept": "application/json"
        },
        "body": "api_key=fixture-secret&format=json"
      },
      "response": {
        "status": 200,
        "headers": {
          "content-type": "application/json"
        },
        "bodyFile": "../responses/invalid-key.json"
      }
    }
  ],
  "expected": {
    "kind": "error",
    "code": "CONNECTOR_UPSTREAM_ERROR"
  }
}
